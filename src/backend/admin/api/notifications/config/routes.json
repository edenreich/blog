{
  "routes": [
    {
      "method": "GET",
      "path": "/notifications/:session_id",
      "handler": "notifications.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/notifications",
      "handler": "notifications.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/notifications/:uuid",
      "handler": "notifications.update",
      "config": {
        "policies": []
      }
    }
  ]
}
