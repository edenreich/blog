apiVersion: v1
kind: Service
metadata:
  name: authentication
  namespace: blog
  labels:
    app: authentication
spec:
  selector:
    app: authentication
    version: "${VERSION}"
  ports:
  - port: 8080
    targetPort: 3000
